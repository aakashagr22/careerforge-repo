version: '3.8'

# Commented out local DB service since we are using Neon cloud database.
# If you want to use a local DB again, uncomment this section and the depends_on block below.
# services:
#   db:
#     image: postgres:16-alpine
#     container_name: careerforge-postgres
#     restart: always
#     ports:
#       - "5432:5432"
#     environment:
#       POSTGRES_DB: careerforge
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgrespassword
#     volumes:
#       - careerforge_pgdata:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres"]
#       interval: 5s
#       timeout: 5s
#       retries: 5

services:
  backend:
    build:
      context: ./careerforge
      dockerfile: Dockerfile
    container_name: careerforge-backend
    restart: always
    ports:
      - "8082:8082"
    env_file:
      - ./careerforge/.env
    # depends_on:
    #   db:
    #     condition: service_healthy

# volumes:
#   careerforge_pgdata:
